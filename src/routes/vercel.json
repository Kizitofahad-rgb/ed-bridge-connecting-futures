{
  "buildCommand": "npm run build",
  "outputDirectory": ".vercel",
  "routes": [
    { "src": "/_vercel/.*", "dest": "/_vercel/$1" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
