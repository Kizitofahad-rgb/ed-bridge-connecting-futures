{
  "buildCommand": "npm run build",
  "outputDirectory": ".vercel/output",
  "functions": {
    "api/server.js": {
      "runtime": "nodejs20.x"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/api/server" }
  ]
}
